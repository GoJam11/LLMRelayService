# 2026-05-24

### 变更
- 将控制台前端（console/ai-proxy-dashboard）主题重新设计为 C4D 卡通风格：蓝色/靛蓝色主色调、圆角卡片（12px）、卡片阴影、彩色数据指标卡（支持 blue/green/amber/purple/cyan 变体）
- 更新全局 CSS 变量（index.css）：主 radius 从 0 改为 0.75rem、背景改为浅蓝色调 `oklch(0.966 0.014 252)`、主色改为鲜明蓝色 `oklch(0.60 0.214 259)`、更新图表颜色为彩虹色谱，卡片添加 box-shadow
- 导航栏重新设计：白色圆角卡片背景 + 阴影、蓝色 AI 徽标图标 + Proxy pill 标签、active 导航项使用 primary 蓝色填充样式
- 页面标题图标改为带彩色背景的圆角容器（primary/10 背景 + ring）
- 登录/Session Shell 页面添加渐变背景（from-background to-primary/5）
- MetricCard 新增 `color` prop（blue/green/amber/purple/cyan/default），各变体带彩色左边框 + 淡色背景 + 彩色数值文字
- Dashboard 页面所有指标卡片按位置循环分配颜色变体
- 更新 card、button、badge、input、dialog、alert、textarea、popover、tooltip、empty、dropdown-menu、select 等 UI 基础组件：移除 `rounded-none`，统一使用圆角样式（button: rounded-lg，badge: rounded-full pill 形状，dialog/popover/dropdown: rounded-xl）

### 变更（布局重构）
- 将控制台前端布局从顶部 TopNav 模式重构为左侧固定 Sidebar 模式，与参考设计图一致
- 左侧 Sidebar：深海军蓝背景（240px 固定宽度）、AI Proxy 品牌 Logo、带图标的竖向导航菜单、底部主题/语言/退出紧凑控件
- 导航图标更新：各页面使用对应语义图标（LayoutDashboard/BarChart2/Building2/Cpu/Network/KeyRound/ScrollText/Code2）
- 主内容区：flex-1 自适应宽度、独立 overflow-y-auto 滚动、移除原有 max-w-430 居中约束
- 整体改用 `h-screen overflow-hidden` + sidebar/main-content 双列布局

### 变更（监控页布局对齐）
- 重构监控面板（DashboardPage）布局，移除嵌套 Card 包裹层，指标卡片直接在首屏可见
- 将路由筛选器、时间范围筛选器、刷新和查看日志按钮合并为标题行右侧紧凑控件栏
- 移除"请求概况"和"时延与稳定性"外层 Card wrapper，改用简洁 section 标签分组
- 系统指标（存储后端）并入缓存健康网格，消除孤立的单卡片网格
- 保持所有功能逻辑不变，仅调整视觉层级和布局结构

### 新增（监控页高还原度补全）
- 监控面板新增请求量趋势面积图（基于 requests 数据按时间分桶，使用 recharts + shadcn ChartContainer），显示总请求数与错误请求数双区域叠加
- 时间分桶规则：1h→5分钟桶，24h→1小时桶，72h→3小时桶，7d/30d→1天桶，all→7天桶；X轴标签随范围自动调整格式
- 监控面板新增最近请求列表（取最新 8 条），展示 HTTP 状态 Badge（颜色语义化）、路由前缀、响应时长、时间戳
- 整体布局更新：概览卡片 → 2列宽（趋势图 3/5 + 请求列表 2/5）→ 延迟指标 → 缓存健康 → 渠道健康
- 新增 i18n 键：`monitor.requestTrend`、`monitor.recentRequests`（中英双语）

### 变更（监控页对齐设计图细节）
- MetricCard 重新设计：移除彩色左边框和背景底色，改为白底干净卡片 + 右侧彩色圆角图标区，视觉更接近设计图风格
- 新增 `icon` prop 支持，各指标卡片传入对应语义图标（Layers/CheckCircle2/AlertTriangle/ArrowLeftRight/Gauge/Zap/Clock/BarChart3）
- 趋势图改为全宽展示，新增 Tab 切换器（请求量 / 成功率 / 延迟），三条数据线均支持切换
- 趋势图数据新增 `successRate`、`avgLatency` 字段，支持三种视图切换
- 底部布局改为「渠道状态表」（左 3/5，含状态点 + 成功率 + 平均延迟 + 迷你折线）+ 「最近日志」（右 2/5，含 Method badge + 路径 + 状态码 + 时长 + 时间）
- 最近日志展示 HTTP Method（GET/POST 色块 badge）、API 路径（shortText 截断）、状态码、时长、时间戳
- 渠道状态列表使用 `stats.routes` 聚合数据（含 requests/errors/avg_duration_ms），迷你折线从 requests 计算每小时桶
- 新增 i18n 键：channelStatus/recentLogs/viewAll/tabRequests/tabSuccessRate/tabLatency/statusHealthy/statusDegraded/statusDown/statusUnknown/channelName/statusLabel/trend

### 变更（C4D 主题二次打磨）
- 将控制台默认主题切到浅色，并将应用壳改为浮动侧边栏 + 玻璃质感主内容区，背景使用浅蓝、青色和琥珀色柔和光感层。
- 更新 Card、Button、PageHeader、MetricCard 的基础样式：卡片增加半透明玻璃、内高光和柔和投影，按钮改为蓝色 C4D 主按钮，指标卡增加底部波形装饰和立体彩色图标。
- 侧边栏增加代码绘制的 C4D 方块品牌标识和底部数据插画，保持现有导航、主题、语言和退出逻辑不变。
- 监控页标题区改为参考图风格的浅色面板，筛选器和操作按钮并入 PageHeader actions，核心数据、图表、渠道状态与最近日志的数据来源保持不变。
- 渠道管理页从桌面卡片网格改为「概览卡片 + 筛选区 + 表格列表」布局，新增本地搜索、类型和启用状态筛选；新增/编辑/删除/测试/导入导出等原有操作入口不变，移动端保留原卡片布局。

### 修复
- 调整监控页右上角 C4D bot 装饰的尺寸、垂直居中和标题区右侧预留空间，避免在宽屏 Chrome 下被标题面板裁切或压到操作按钮。
