# 2026-05-20

### 修复
- 增加上游响应 body 空闲超时保护：`UPSTREAM_REQUEST_TIMEOUT_MS` 继续只约束等待上游响应头，新增 `UPSTREAM_RESPONSE_IDLE_TIMEOUT_MS` 约束响应体读取阶段，避免上游已返回 header 但后续无 body/chunk 时客户端无限挂起；流式响应只在长时间没有任何 chunk 时才断开。
