# 2026-05-25

### 变更
- 重新生成侧栏底部 C4D 装饰图，替换为透明背景且细节更简化的 LRS 聚合概念 PNG，并将侧栏从 CSS 绘制图形切换为项目内图片资产。
- 新增项目级 `llmrelayservice-openapi` skill，提供通过 `/api/v1` 管理 LLMRelayService 的流程、端点参考和本地认证缓存 helper；认证文件位于 skill 目录内并由 `.gitignore` 排除。

### 修复
- 缩小监控页「请求量趋势」图表高度，去掉外层过高的最小高度，并将有数据和空态高度统一为 160px，减少首屏纵向占用。
