# 2026-05-02

### 变更

- **数据库迁移改造**：将迁移从 Dockerfile CMD 移到 `server.ts` 启动时执行，失败不阻断服务，显示降级指引页。
  - `src/db/migrate.ts`：去掉 inline migrations，改用 `drizzle-orm/postgres-js/migrator` 的 `migrate()`；新增 `MigrationStatus` 类型；`runMigrations()` 添加 `force` 参数绕过缓存。
  - `src/server.ts`：启动时调用 `runMigrations()`；`Bun.serve()` 拦截 `/health`（返回 503）、`/`（返回 HTML 指引页）、`POST /api/db/reset`（清除数据并重建）；新增 `resetDatabase()`、`showMigrationGuide()`。
  - `src/degraded.html`：独立 HTML 模板文件，`{{ERROR}}` 占位符。
  - `Dockerfile`：`CMD ["bun", "run", "src/server.ts"]`。
- **环境变量合并**：`PASSWORD` 合并到 `GATEWAY_API_KEY`，统一认证入口。
  - `src/console-ui.ts`：`getPassword()` → `getConsolePassword()`（读取 `GATEWAY_API_KEY`）；登录接口同时支持 `password` 和 `gatewayKey` 字段；所有错误提示更新为 `GATEWAY_API_KEY`。
  - `.env.example`：删除 `PASSWORD` 变量。
  - `README.md`：更新环境变量说明，`GATEWAY_API_KEY` 同时作为网关认证密钥和控制台登录密码。
  - 前端 i18n（zh/en）：登录页文案更新为 `GATEWAY_API_KEY`。
