# 2026-04-29

### 变更

- 更新 README，说明 Responses API 兼容层功能：当渠道配置 `responsesMode: chat_compat` 时，LRS 会将客户端发来的 OpenAI Responses API（`/v1/responses`）请求自动转换为 Chat Completions 格式转发给上游，并将响应转换回 Responses 格式，以支持接入 Codex CLI / Codex App 等客户端
- README 新增独立章节"Responses API 兼容层（接入 Codex App）"，包含 `responsesMode` 三种取值说明及 JSON 配置示例
