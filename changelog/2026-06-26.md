# 2026-06-26

### 变更
- Console header 对齐 LRS Clear 风格：改为顶部横向导航，移除黑夜模式与多语言入口，保留 LRS 品牌、实时状态、API/配置/退出操作。
- Console 固定浅色中文：移除启动时按本地存储/系统偏好切换 dark class 的脚本，i18n 默认固定 `zh`。
- 将“模型”独立页面并入“路由”页：模型管理作为路由页内 tab，`#/models` 兼容跳转到 `#/routes?tab=models`。
