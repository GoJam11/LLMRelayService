# 2026-05-03

### 变更

- **Schema 优化 + 迁移文件**：删除 `console_request_cache_points` 表（缓存调试功能下线），为 `console_requests` 表添加 `api_key_id` 索引。
  - `src/db/schema.ts`：删除 `consoleRequestCachePoints` 表定义；添加 `apiKeyIdIdx` 索引。
  - `drizzle/0001_ambiguous_maria_hill.sql`：迁移文件，包含删表和建索引。
  - `drizzle/meta/`：同步更新 snapshot 和 journal。
- **AGENTS.md 规范更新**：新增规则——schema 改动和迁移文件必须在同一 commit 中提交。
- **OpenAPI REST API**：新增一套与面板功能对齐的 REST API，使用 Bearer Token 认证（Token 即 `GATEWAY_API_KEY`）。
  - `src/openapi-routes.ts`：新建，21 个端点（health、providers、requests、stats、keys、aliases），含 Bearer 认证中间件。
  - `src/index.ts`：注册 `registerOpenApiRoutes(app)`，放在 `registerConsoleRoutes` 之前避免 catch-all 拦截。
  - 前端 `api-docs-page.tsx`：新建 API 文档页面，展示端点列表、认证说明、搜索过滤、可展开 body 示例。
  - 前端 `nav-bar.tsx`、`App.tsx`、`use-hash-route.ts`：添加 `"api"` 页面路由和导航按钮。
