# 2026-05-31

### 新增

- 为 managed API key 增加累计 Token 额度：新增 `token_quota` / `token_used` / `quota_charged_tokens` 迁移字段，历史请求会在迁移时回填已用额度。
- 网关在 managed API key 达到额度后返回 429，管理员环境变量 key 不受分发额度限制。
- 控制台 Keys 页面增加额度展示和编辑弹窗，支持设置非负整数 Token 上限，留空表示不限制。
- OpenAPI 和控制台 API 增加 `PATCH /keys/:id/quota` 管理接口。
