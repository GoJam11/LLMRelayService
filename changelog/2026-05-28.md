# 2026-05-28

### 新增
- 新增全站回退策略，支持配置重试次数、同模型换渠道、任意模型回退，以及超时、网络错误、408、429、5xx 等触发条件。
- 在路由页面新增回退策略配置卡片，并提供对应的 Console 管理 API 读写 `gateway.failover` 设置。
- 回退策略支持按请求传入的模型名配置自定义 fallback 模型数组，命中后优先于全站回退模式执行。

### 变更
- 模型路由支持返回按优先级排序的候选路由，代理转发在最终尝试日志中记录回退链路、原始路由和回退原因。
