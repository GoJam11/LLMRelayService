# 2026-04-27

### 新增
- Responses ↔ Chat Completions 兼容层：将上游返回内容中的 `<think>...</think>` 标签转换为 OpenAI Responses 原生 reasoning 输出项（`type: "reasoning"`），而非拼进 `output_text`
  - 非流式路径：解析 assistant content 中的 think 标签，拆分出 reasoning item（含 `content[].type = "reasoning_text"`）和普通 message item
  - SSE 流式路径：引入增量 think 标签解析器，将流式 delta 拆分到对应的 `response.reasoning_text.delta` 和 `response.output_text.delta` 事件，不再透传 `<think>` 字面量
  - 新增两个聚焦测试用例覆盖以上两条路径

### 修复
- Responses 转 Chat Completions 兼容转换在目标为 MiniMax 时自动降级为 MiniMax 兼容 payload，移除 `store`、`parallel_tool_calls`、默认 `tool_choice` 和 tool `strict:false` 等会触发 invalid chat setting 的字段，并合并连续 system/developer 系统消息。
