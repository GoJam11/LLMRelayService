# 2026-09-07

### 新增

- **控制台业务复合组件层（Composite Business Patterns）与设计系统展厅**：
  - **复合组件抽取 (`src/components/patterns/*`)**：
    - `TestStatusButton`：收敛全站 3 状态测试按钮（空闲/测试中/成功带延迟 Tooltip/失败带错误信息 Tooltip），消除原本各页面数十行重复三元样板代码。
    - `TableRowActions`：规范数据表格行内操作列（测试/编辑/动作/删除），统一样式尺寸 `size="xs"`、右对齐与间距。
    - `StatusBadge`：统一样式指示器，支持 Dot 模式（含脉冲动画）与 Badge 模式，涵盖 success/warning/destructive/muted/info 语义。
    - `FilterCard`：标准化筛选卡片容器，避免各页面额外嵌套多余边框。
    - `ConfirmDialog`：统一删除与危险操作二次确认对话框规范。
  - **内置设计系统展厅 (`#/design`)**：新增 `DesignShowcasePage` 及 NavBar/Settings 入口，集中提供交互式按钮状态机沙盒、标准表格排版、状态徽章和表单弹窗范例。

### 变更

- **文档重构：全面重写中英文 README（README.md / README.en.md）**：重塑项目定位为专为个人开发者打造的智能 LLM 流量中枢与自愈网关。重点梳理并深化两大核心卖点：
  - **超强路由调度与自愈体系**：新增 Mermaid 调度与容灾自愈全景流程图，系统展现模型自动路由、显式渠道定向、协议强行路由与可见性隔离（`direct` / `explicit_only`）；深入阐述虚拟模型别名（Alias）多目标绑定、真实模型遮蔽与独立策略；详述多级 Failover（渠道内重试、`same_model` 跨渠道平移、自定义模型降级）及细粒度触发条件；补充 Codex 双向协议互转（`responsesMode: chat_compat`）与 Claude Code 伪装穿透（`claudeCodeCompat`）。
  - **极致贴合个人开发者场景**：强调 SQLite 单文件持久化零运维负担（单容器 5 秒启动）、个人工具链集中收拢（Cursor / Claude Code / Codex / Python SDK 一键接入配置）、Managed API Key 细粒度模型白名单与美元费用硬额度拦截（从物理层面杜绝 Agent 死循环刷爆账单）、显微镜级全文排障日志与透明 Prompt Cache 公式级计费精算。
- **页面规范重构**：`models-page.tsx`、`routes-page.tsx`、`keys-page.tsx` 切换至 `TestStatusButton`、`TableRowActions`、`StatusBadge` 与规范 `PageHeader`，消除多处非标准内联样式与裸属性。
- **同步更新 AI 设计指南技能**：在 `.agents/skills/ai-proxy-dashboard-design-guide/SKILL.md` 中补充模式组件标准调用方法。
